#include <iostream>
#include <vector>
#include <string>

/*

Example 1:

Input: s = "babad"
Output: "bab"
Explanation: "aba" is also a valid answer.
Example 2:

Input: s = "cbbd"
Output: "bb"

The definition of a palindrome is a string that can be read the same even in reverse.


Solution:


*/

class Solution {
public:
    std::string longestPalindrome(std::string s) {
        
    }
};

int main(){
    Solution solve;
    std::string s;
    std::cin >> s;
    solve.longestPalindrome(s);
}